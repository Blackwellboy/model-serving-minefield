# Reviewed false positives for the private sanitizer kit's supplementary
# shape scan, passed with --adjudicated. Format: relpath:lineno: reason
#
# This file records judgements. It hides nothing: every line below is still
# printed by the scanner, in its own ADJUDICATED section, with the reason
# attached. It never weakens a pattern. If a line here stops being true,
# delete it and the hit comes back as a hit.
#
# Adjudicated 2026-07-28, session registry-integrity-ci.

mining/2026-07-28-agreement-floor-data/raw/nodeA_srv1_pass2.jsonl:270: em dash inside a model-generated answer in committed raw evidence. The no-dashes rule governs text WE write; rewriting a model's output would falsify the raw, which is the one thing this file exists to preserve.
